# Desenvolva um programa que pergunte ao usuário quantas vezes ele quer que uma mensagem seja exibida,
# e depois use um for para imprimir essa mensagem repetidas vezes.
nome= int(input('Digite quantas vezes deseja que a mensagem apareceça: '))
numero = nome

for i in range(numero):
    
    
    print(f'Você tem que se esforça mais ')
else:
    ('Número invalido')
